{% extends 'layouts/base.html.twig' %}

{% block title 'Share a post' %}

{% block body %}
    <h1 class="h2 mb-4">Share "{{ post.title }}" via e-mail</h1>

    <div class="row">
        <div class="col-md-6">
            {{ form_start(shareForm) }}
                {{ form_widget(shareForm) }}

                <button type="submit" class="btn btn-primary w-100" formnovalidate>Share Post</button>
            {{ form_end(shareForm) }}
        </div>
    </div>

{% endblock %}